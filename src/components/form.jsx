
export default function Form({ input, call, store }) {

	const {
		username,
		email,
		password,
		isAuthenticated,

		setUsername,
		setEmail,
		setPassword,
		setIsAuthenticated
	} = store

	const formType = () => {
		
	}

}